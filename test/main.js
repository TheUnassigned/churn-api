/**
 * Main mocha entry file for running tests
 * -- This should be the only file in the root test directory --
 */

//import { or } from '../utils/promise_helpers'

/**
 * GLOBALS
 */
// root folder mocha is run from
global.rootDir = __dirname + '/..'
// file helpers
/*global.fileHelpers = {
  findFiles
}*/
